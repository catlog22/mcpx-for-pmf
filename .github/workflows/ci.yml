# Lightweight CI on main/PR — does not publish releases.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - name: Test
        run: go test ./... -count=1
      - name: Format
        run: test -z "$(gofmt -l ./cmd ./internal)"
      - name: Vet
        run: go vet ./...
      - name: Race test
        run: go test -race ./... -count=1
      - name: Build with provenance
        shell: bash
        run: |
          BUILD_COMMIT="$(git rev-parse HEAD)"
          BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          CGO_ENABLED=0 go build -ldflags "-X main.commit=${BUILD_COMMIT} -X main.date=${BUILD_DATE}" -o bin/mcpx-server ./cmd/mcpx-server
          VERSION_OUTPUT="$(./bin/mcpx-server -version)"
          [[ "${VERSION_OUTPUT}" == *"commit=${BUILD_COMMIT}"* ]]
          [[ "${VERSION_OUTPUT}" != *"date=unknown"* ]]
