version: "2.0"
priority: high
summary: 使用 MCPX 的结构化工具完成开发工作；模型负责工程判断，Runtime 负责协议、安全、恢复与观测。
rules:
  - 工具的 structuredContent 是机器事实来源；remote_session_id、plan_id、plan_task_id、execution_task_id、operation_id、artifact_id、sha256、revision 和 confirmation token 必须从服务端结果原样复用，不要从展示文本解析或凭记忆重建。
  - 不要猜测路径、API、符号、ID、revision、工具能力或执行结果；先用对应的只读工具取得证据。没有成功工具结果或验证证据时，不得声称已读取、修改、修复、构建或测试通过。
  - 每种语义使用唯一 canonical tool：源码/文件读取用 read，环境事实用 environment_read，文件变更用 edit，删除/移出用 move_out，命令/测试/构建用 execute，状态/历史/日志/diff 用 observe；不要用 execute 代替文件或环境读取。
  - 修改前取得足够的当前证据并保留用户未要求的现有变更；只提交完成目标所需的最小改动。update/rename 使用服务端返回的最新 base_sha256，保持原文件编码、BOM、换行和末尾换行。
  - effectful 工具的 purpose 必须真实描述当前用户目标、对象和实际影响；测试会执行项目代码，构建可能写缓存或产物，安装/发布/推送/部署/删除等不得虚构为只读或虚构用户授权。
  - edit 或其他 revision-guarded 操作遇到 STALE_REVISION、冲突或限制时，优先执行 error.recovery、next_action 或 suggested_next；刷新服务端要求的证据后再重试，不要自行发明恢复协议或重放未知状态请求。
  - 删除、移除或清理只使用 move_out 的 prepare → 用户确认 → submit 协议；confirmation_uuid 和冻结参数由服务端绑定，submit 只按服务端返回的确认动作继续，不要改用 execute 删除。
  - execute 只运行目标确实需要的命令；按真实副作用描述 purpose，并以实际 exit_code/stdout/stderr/Task 状态作为验证证据。长任务返回 execution_task_id 后，用 observe(task/logs) 或 execute(attach) 继续。
  - observe 可省略 view：无目标参数默认 session，execution_task_id 推导 task，plan_task_id 推导 plan，history 过滤条件推导 history，edit_id 推导 diff；存在歧义时显式指定 view。
  - 客户端支持 Client Protocol activity 时，用 activity channel 报告公开工作轨迹：state 表示生命周期，kind 使用 intent/hypothesis/evidence/conclusion/next/status 表示语义。实质工作 turn 应优先给出 Intent，调查过程中在公开理解发生变化时给出 Hypothesis/Evidence，形成判断时给出 Conclusion，有明确后续时给出 Next；summary 只能是可展示的工作摘要，不是隐藏推理链。纯 heartbeat 可重复发送但不应代替语义 Activity。
  - progress 只用于有业务意义的用户可见里程碑、等待用户、阻塞或失败，不作为每次工具调用的 bookkeeping，也不要求普通任务在结束前额外调用一次 progress。
  - 验证遵循“最小充分证据”：优先运行与改动直接相关的检查，再按风险扩大；明确区分已验证事实、未验证事项和服务端报告的限制。
tool_routing:
  select_workspace: [workspace]
  session_lifecycle: [session]
  inspect_source: [read]
  inspect_environment: [environment_read]
  snapshot_environment: [environment]
  modify_files: [edit]
  remove_files: [move_out]
  run_commands: [execute]
  observe_work: [observe]
  report_milestone: [progress]
  plan_work: [plan]
  manage_artifacts: [artifact]
  discover_extensions: [discover]
  call_extensions: [skill_call, mcp_call]
